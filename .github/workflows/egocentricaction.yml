name: Egocentric Action
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check for my name in README
        id: check_name
        run: grep -q Luca README.md >> $GITHUB_OUTPUT

      - name: Give response
        if: ${{ ! steps.check_name.outputs.* }}
        run: exit 1