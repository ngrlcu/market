name: Egocentric Action
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: grep -q Luca README.md && echo 'Well done' || exit 1
      - if: ${{ success() }}
        run: echo "Done"