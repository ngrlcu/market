name: Egocentric Action
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "check_state=$( grep -q Luca README.md && echo "Well done" || exit 1"
      - if: ${{ success() }}