name: Egocentric Action
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: grep -q Luca README.md | COMMAND "check_state=$(cat -)" >> "$GITHUB_ENV"
      - if: ${{ ! $check_state }}
        run: exit 1